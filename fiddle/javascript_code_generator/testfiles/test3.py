# create a javascript file that creates 2
# 2 dimension arrays and can add, subtract
# and multiply them


import generator.generator as codegen
import generator.fileHandler as filegen

FILENAME = "matrix"

# step 1: create 2 multi dimension arrays and print them

content = codegen.

filegen.generate_file(FILENAME, content)
filegen.execute_file(FILENAME)

# step 2: print the arrays => recursive?

# step 3: create the addition

# step 4: create the subtract
